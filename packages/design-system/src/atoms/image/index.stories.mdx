import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import Image from "./index.js";

<Meta
  title="Atoms/Image"
  component={Image}
  argTypes={{}}
/>

# Lazy Cover Image

A `<Image>` lazily loads and displays an image that will cover its parent container. 
It is meant to be used with `<Suspense>`. Optionally, it can be used with `<AspectRatioContainer>`.

<Canvas>
  <Story
    name="Default"
    args={{
      src: 'https://cwoodley.com/img/mybpstation-com.jpg',
      alt: 'my bp station'
    }}
  >
    {(args) => <Image {...args} />}
  </Story>
</Canvas>

<ArgsTable of={Image} />
