import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import AspectRatioContainer from "./index.js";

<Meta
  title="Atoms/Aspect Ratio Container"
  component={AspectRatioContainer}
  argTypes={{}}
/>

# Aspect Ratio Container

An `<AspectRatioContainer>` displays a container constrained to an aspect ratio.

<Canvas>
  <Story
    name="Default"
    args={{}}
  >
    {(args) => <AspectRatioContainer {...args} />}
  </Story>
</Canvas>

<ArgsTable of={AspectRatioContainer} />