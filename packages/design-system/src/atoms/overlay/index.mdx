import { Meta, Controls } from '@storybook/blocks';
import * as OverlayStories from './index.stories';

<Meta of={OverlayStories} />

# Overlay

An `Overlay` component displays a "backdrop" over existing content 
in order to display new content. Example of new content can be an application modal or a 
title and description of a thumbnail image.

```
<Overlay>
  your new content here...
</Overlay>
```

## Props

<Controls />